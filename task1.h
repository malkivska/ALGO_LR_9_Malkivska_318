#ifndef TASK1_H  // Перевірка, чи не була вже включена ця бібліотека
#define TASK1_H  // Якщо не була, визначаємо її

// Оголошення функції customReplace, яка реалізує заміну частини рядка
void customReplace(char* source, size_t pos, size_t len, const char* insert, size_t subpos, size_t sublen);

// Завершуємо захист від подвійного включення
#endif // TASK1_H
